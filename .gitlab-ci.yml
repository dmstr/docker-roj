stages:
  - build
  - test
  - cleanup
before_script:
  - export COMPOSE_PROJECT_NAME=buildpipeline${CI_PIPELINE_ID}
  - export BUILD_ROJ_VERSION=$(git describe --always --long --dirty)
build:
  stage: build
  script:
    - docker-compose build
test:
  stage: test
  script:
    - docker-compose run roj
    - docker-compose run roj help.sh
    - docker-compose run roj docker-machine ls
cleanup:
  stage: cleanup
  script:
    - docker-compose down -v --remove-orphans
  when: always